import React from 'react'

export default function App(){
    return (
        <main style={{fontFamily:'system-ui, sans-serif', padding: '2rem', lineHeight:1.6}}>
            <h1>Cool Site</h1>
            <p>Hosted with GitHub pages, dependencies with npm, and front-end with react.</p>
            <h2>Note</h2>
            <p>Use npm run dev in root to localhost the page.</p>
        </main>
    )
}